book_nokia_logo_transparent.png
tables-over-1000.txt

# Brand-monitor stack: session files are credentials, the shared SQLite DB
# holds raw (un-redacted) messages + breach details + lookalike domains.
# Only the redacted *.json snapshots ever get committed.
monitor.session
monitor.session-journal
*.session
*.session-journal
monitor.db
monitor.db-journal
telegram-monitor.db
telegram-monitor.db-journal
.venv/
.DS_Store
notify-mentions.json

# Per-session Claude Code state. .claude/skills/ IS committed (shared via
# git); .claude/session.id is per-checkout and per-session and shouldn't be.
.claude/session.id
